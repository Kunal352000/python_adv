"""
working with array:-
---------------------
    array is a data structure whic allows similar type of data.

    if we want to working with arrays to required array module

    if we want to display all the properties of array module module by using
    follow command
                import array
                print(dir(array))
            """
import array
print(dir(array))

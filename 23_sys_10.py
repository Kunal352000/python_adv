"""command line argument
    whenever we are passing the argument to the python file through the command
    line propmt at runtime,that type of arguments are called command line
    arguments

    in python we can implement the command line argument by using argv variable
    of sys module

    argv variable to return the list object

    in python,by default filename is a one command line argument

wap to print command line argument
"""
import sys
print(sys.argv)

